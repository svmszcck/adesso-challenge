import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  noData: {
    flex: 1,
    alignItems: "center",
  },
  image: {
    width: 250,
    height: 250,
    aspectRatio: 1,
  },
});

export default styles;
